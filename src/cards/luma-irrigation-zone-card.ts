import{LitElement,css,html,nothing}from"lit";import{customElement,property,state}from"lit/decorators.js";import{runAction}from"../helpers";import{lumaTokens}from"../styles";import type{HomeAssistant,LovelaceCard}from"../types";
interface Config{type:string;entity:string;name:string;icon?:string;role?:string;duration_entity:string;start_entity?:string;stop_entity?:string;progress_entity?:string;remaining_entity?:string;program_state_entity?:string}
@customElement("luma-irrigation-zone-card")export class LumaIrrigationZoneCard extends LitElement implements LovelaceCard{@property({attribute:false})hass?:HomeAssistant;@state()private config?:Config;@state()private pending=false;private timer?:number;static styles=[lumaTokens,css`ha-card{padding:17px;border:1px solid color-mix(in srgb,var(--tone) 20%,transparent);border-radius:20px;background:linear-gradient(145deg,color-mix(in srgb,var(--tone) 10%,var(--luma-surface)),var(--luma-surface) 70%);box-shadow:var(--luma-shadow)}.head{display:grid;grid-template-columns:46px 1fr auto;align-items:center;gap:12px}.icon{display:grid;place-items:center;width:46px;height:46px;border-radius:15px;color:var(--tone);background:color-mix(in srgb,var(--tone) 16%,transparent)}.name{font-size:16px;font-weight:730}.sub{margin-top:3px;color:var(--luma-muted);font-size:10px}.start{padding:8px 12px;border:0;border-radius:999px;color:var(--tone);background:color-mix(in srgb,var(--tone) 14%,transparent);font-size:11px;font-weight:730}.meta{display:flex;align-items:center;justify-content:space-between;margin-top:14px;color:var(--luma-muted);font-size:11px}.duration{display:inline-grid;grid-template-columns:26px minmax(54px,auto) 26px;align-items:center;border-radius:999px;background:color-mix(in srgb,var(--tone) 9%,transparent);overflow:hidden}.duration button{display:grid;place-items:center;width:26px;height:28px;padding:0;border:0;color:var(--tone);background:transparent;font-size:16px;font-weight:700}.duration span{text-align:center;color:var(--primary-text-color);font-weight:680}.track{height:7px;margin-top:8px;border-radius:99px;background:color-mix(in srgb,var(--primary-text-color) 8%,transparent);overflow:hidden}.fill{height:100%;border-radius:inherit;background:var(--tone);transition:width .4s ease}.inactive .track{display:none}button{cursor:pointer}`];setConfig(c:Config){if(!c?.entity||!c.duration_entity)throw Error("entity and duration_entity required");this.config=c}getCardSize(){return 2}private async changeDuration(delta:number){if(!this.hass||!this.config)return;const e=this.hass.states[this.config.duration_entity],current=Number(e?.state),step=Number(e?.attributes.step)||1,min=Number(e?.attributes.min),max=Number(e?.attributes.max);if(!Number.isFinite(current))return;const value=Math.min(Number.isFinite(max)?max:Infinity,Math.max(Number.isFinite(min)?min:-Infinity,current+delta*step));await this.hass.callService("number","set_value",{value},{entity_id:this.config.duration_entity})}private activate(){if(!this.pending){this.pending=true;clearTimeout(this.timer);this.timer=window.setTimeout(()=>this.pending=false,3500);return}this.pending=false;const c=this.config!,on=this.hass!.states[c.entity]?.state==="on",button=on?c.stop_entity:c.start_entity;if(button){void this.hass!.callService("button","press",undefined,{entity_id:button});return}void runAction(this,this.hass!,{action:"toggle",entity:c.entity},c.entity)}render(){if(!this.hass||!this.config)return nothing;const c=this.config,s=this.hass.states,on=s[c.entity]?.state==="on",duration=s[c.duration_entity]?.state||"—",p=Math.max(0,Math.min(100,Number(s[c.progress_entity||""]?.state)||0)),remaining=s[c.remaining_entity||""]?.state,tone=on?"var(--info-color,var(--primary-color))":"var(--primary-color)";return html`<ha-card class=${on?"":"inactive"} style=${`--tone:${tone}`}><div class="head"><span class="icon"><ha-icon icon=${c.icon||"mdi:sprinkler-variant"}></ha-icon></span><span><div class="name">${c.name}</div><div class="sub">${on?(remaining?`${remaining} hátra`:"Öntözés folyamatban"):(c.role||"Állítsd be az időt, majd indítsd el")}</div></span><button class="start" @click=${()=>this.activate()}>${this.pending?"MEGERŐSÍTÉS":on?"LEÁLLÍTÁS":"INDÍTÁS"}</button></div><div class="meta"><span class="duration"><button aria-label="Idő csökkentése" @click=${()=>this.changeDuration(-1)}>−</button><span>${duration} perc</span><button aria-label="Idő növelése" @click=${()=>this.changeDuration(1)}>+</button></span>${on?html`<span>${Math.round(p)}%</span>`:nothing}</div><div class="track"><div class="fill" style=${`width:${p}%`}></div></div></ha-card>`}}
