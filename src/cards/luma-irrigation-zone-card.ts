import{LitElement,css,html,nothing}from"lit";import{customElement,property,state}from"lit/decorators.js";import{runAction}from"../helpers";import{lumaTokens}from"../styles";import type{HomeAssistant,LovelaceCard}from"../types";
interface Config{type:string;entity:string;name:string;icon?:string;role?:string;duration_entity:string;progress_entity?:string;remaining_entity?:string;program_state_entity?:string}
@customElement("luma-irrigation-zone-card")export class LumaIrrigationZoneCard extends LitElement implements LovelaceCard{@property({attribute:false})hass?:HomeAssistant;@state()private config?:Config;@state()private pending=false;private timer?:number;static styles=[lumaTokens,css`ha-card{padding:17px;border:1px solid color-mix(in srgb,var(--tone) 20%,transparent);border-radius:20px;background:linear-gradient(145deg,color-mix(in srgb,var(--tone) 10%,var(--luma-surface)),var(--luma-surface) 70%);box-shadow:var(--luma-shadow)}.head{display:grid;grid-template-columns:46px 1fr auto;align-items:center;gap:12px}.icon{display:grid;place-items:center;width:46px;height:46px;border-radius:15px;color:var(--tone);background:color-mix(in srgb,var(--tone) 16%,transparent)}.name{font-size:16px;font-weight:730}.sub{margin-top:3px;color:var(--luma-muted);font-size:10px}.start{padding:8px 12px;border:0;border-radius:999px;color:var(--tone);background:color-mix(in srgb,var(--tone) 14%,transparent);font-size:11px;font-weight:730}.meta{display:flex;justify-content:space-between;margin-top:14px;color:var(--luma-muted);font-size:11px}.track{height:7px;margin-top:8px;border-radius:99px;background:color-mix(in srgb,var(--primary-text-color) 8%,transparent);overflow:hidden}.fill{height:100%;border-radius:inherit;background:var(--tone);transition:width .4s ease}.inactive .track{display:none}button{cursor:pointer}`];setConfig(c:Config){if(!c?.entity||!c.duration_entity)throw Error("entity and duration_entity required");this.config=c}getCardSize(){return 2}private activate(){if(!this.pending){this.pending=true;clearTimeout(this.timer);this.timer=window.setTimeout(()=>this.pending=false,3500);return}this.pending=false;void runAction(this,this.hass!,{action:"toggle",entity:this.config!.entity},this.config!.entity)}render(){if(!this.hass||!this.config)return nothing;const c=this.config,s=this.hass.states,on=s[c.entity]?.state==="on",duration=s[c.duration_entity]?.state||"—",p=Math.max(0,Math.min(100,Number(s[c.progress_entity||""]?.state)||0)),remaining=s[c.remaining_entity||""]?.state,tone=on?"var(--info-color,var(--primary-color))":"var(--primary-color)";return html`<ha-card class=${on?"":"inactive"} style=${`--tone:${tone}`}><div class="head"><span class="icon"><ha-icon icon=${c.icon||"mdi:sprinkler-variant"}></ha-icon></span><span><div class="name">${c.name}</div><div class="sub">${on?(remaining?`${remaining} hátra`:"Öntözés folyamatban"):(c.role||"Kattints az indításhoz")}</div></span><button class="start" @click=${()=>this.activate()}>${this.pending?"MEGERŐSÍTÉS":on?"LEÁLLÍTÁS":"INDÍTÁS"}</button></div><div class="meta"><span>${duration} perc</span>${on?html`<span>${Math.round(p)}%</span>`:nothing}</div><div class="track"><div class="fill" style=${`width:${p}%`}></div></div></ha-card>`}}
