import{LitElement,css,html,nothing}from"lit";import{customElement,property,state}from"lit/decorators.js";import{runAction}from"../helpers";import{lumaTokens}from"../styles";import type{HomeAssistant,LovelaceCard}from"../types";
interface Config{type:string;name:string;icon?:string;color?:string;enabled_entity:string;start_entity:string;day_entities:string[]}
@customElement("luma-irrigation-schedule-card")export class LumaIrrigationScheduleCard extends LitElement implements LovelaceCard{@property({attribute:false})hass?:HomeAssistant;@state()private config?:Config;static styles=[lumaTokens,css`ha-card{padding:17px;border:1px solid color-mix(in srgb,var(--tone) 18%,transparent);border-radius:20px;background:linear-gradient(145deg,color-mix(in srgb,var(--tone) 9%,var(--luma-surface)),var(--luma-surface) 68%);box-shadow:var(--luma-shadow)}.head{display:grid;grid-template-columns:44px 1fr auto;align-items:center;gap:11px}.icon{display:grid;place-items:center;width:44px;height:44px;border-radius:14px;color:var(--tone);background:color-mix(in srgb,var(--tone) 15%,transparent)}.name{font-size:16px;font-weight:720}.time{color:var(--tone);font-size:18px;font-weight:760}.enabled{margin-top:2px;color:var(--luma-muted);font-size:10px}.days{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:5px;margin-top:15px}.day{display:grid;place-items:center;min-width:0;height:34px;padding:0;border:1px solid color-mix(in srgb,var(--tone) 14%,transparent);border-radius:10px;color:var(--luma-muted);background:transparent;font-size:11px;font-weight:760}.day.on{color:var(--tone);background:color-mix(in srgb,var(--tone) 15%,transparent);border-color:color-mix(in srgb,var(--tone) 28%,transparent)}button{cursor:pointer}@media(max-width:380px){.days{gap:3px}.day{height:32px;border-radius:9px}}`];setConfig(c:Config){if(!c?.enabled_entity||c.day_entities?.length!==7)throw Error("enabled_entity and seven day_entities required");this.config=c}getCardSize(){return 2}private toggle(id:string){void runAction(this,this.hass!,{action:"toggle",entity:id},id)}render(){if(!this.hass||!this.config)return nothing;const c=this.config,s=this.hass.states,on=s[c.enabled_entity]?.state==="on",raw=s[c.start_entity]?.state||"—",time=raw.length>=5?raw.slice(0,5):raw,labels=["H","K","Sze","Cs","P","Szo","V"];return html`<ha-card style=${`--tone:${c.color||"var(--primary-color)"}`}><div class="head"><button class="icon" @click=${()=>this.toggle(c.enabled_entity)} aria-label="Ütemezés kapcsolása"><ha-icon icon=${c.icon||"mdi:calendar-clock"}></ha-icon></button><span><div class="name">${c.name}</div><div class="enabled">${on?"Automatika bekapcsolva":"Automatika kikapcsolva"}</div></span><button class="time" @click=${()=>runAction(this,this.hass!,{action:"more-info"},c.start_entity)}>${time}</button></div><div class="days">${c.day_entities.map((id,i)=>html`<button class=${`day ${s[id]?.state==="on"?"on":""}`} @click=${()=>this.toggle(id)}>${labels[i]}</button>`)}</div></ha-card>`}}
