import{LitElement,css,html,nothing}from"lit";import{customElement,property,state}from"lit/decorators.js";import{runAction}from"../helpers";import{lumaTokens}from"../styles";import type{HomeAssistant,LumaAction,LovelaceCard}from"../types";
interface Config{type:string;name:string;subtitle?:string;icon?:string;color?:string;tap_action:LumaAction}
@customElement("luma-navigation-card")export class LumaNavigationCard extends LitElement implements LovelaceCard{@property({attribute:false})hass?:HomeAssistant;@state()private config?:Config;static styles=[lumaTokens,css`ha-card{display:grid;grid-template-columns:42px minmax(0,1fr) 18px;grid-template-areas:"icon name arrow" "icon subtitle arrow";align-items:center;gap:3px 11px;min-height:92px;padding:16px;border:1px solid color-mix(in srgb,var(--tone) 14%,transparent);border-radius:20px;background:linear-gradient(145deg,color-mix(in srgb,var(--tone) 7%,var(--luma-surface)),var(--luma-surface) 72%);box-shadow:0 11px 32px rgba(0,0,0,.05);transition:.17s ease}ha-card:hover{transform:translateY(-2px);box-shadow:0 16px 38px color-mix(in srgb,var(--tone) 10%,transparent)}.icon{grid-area:icon;display:grid;place-items:center;width:42px;height:42px;border-radius:14px;color:var(--tone);background:color-mix(in srgb,var(--tone) 13%,transparent)}.icon ha-icon{--mdc-icon-size:21px}.name{grid-area:name;align-self:end;font-size:14px;font-weight:720}.subtitle{grid-area:subtitle;align-self:start;color:var(--luma-muted);font-size:10px}.arrow{grid-area:arrow;color:var(--luma-muted)}.arrow ha-icon{--mdc-icon-size:17px}`];setConfig(c:Config){if(!c?.name||!c?.tap_action)throw Error("name and tap_action required");this.config=c}getCardSize(){return 2}render(){if(!this.hass||!this.config)return nothing;return html`<ha-card class="interactive" style=${`--tone:${this.config.color||"var(--primary-color)"}`} @click=${()=>runAction(this,this.hass!,this.config?.tap_action)}><span class="icon"><ha-icon icon=${this.config.icon||"mdi:arrow-right-circle-outline"}></ha-icon></span><span class="name">${this.config.name}</span><span class="subtitle">${this.config.subtitle||""}</span><span class="arrow"><ha-icon icon="mdi:arrow-right"></ha-icon></span></ha-card>`}}
