import { LitElement, css, html } from "lit";import{customElement,state}from"lit/decorators.js";import{lumaTokens}from"../styles";import type{LovelaceCard}from"../types";
interface Config{type:string;url:string;height?:number;title?:string}
@customElement("luma-iframe-card")export class LumaIframeCard extends LitElement implements LovelaceCard{@state()private config?:Config;static styles=[lumaTokens,css`ha-card{position:relative;border:1px solid var(--luma-border);border-radius:22px;background:var(--luma-surface);box-shadow:var(--luma-shadow);overflow:hidden}.loading{position:absolute;inset:0;display:grid;place-items:center;color:var(--luma-muted);font-size:12px;background:linear-gradient(135deg,color-mix(in srgb,var(--primary-color) 5%,var(--luma-surface)),var(--luma-surface))}iframe{position:relative;display:block;width:100%;height:var(--height);border:0;background:transparent}`];setConfig(c:Config){if(!c?.url)throw Error("url required");this.config={height:350,...c}}getCardSize(){return 6}render(){return html`<ha-card><div class="loading">Térkép betöltése…</div><iframe title=${this.config?.title||"Beágyazott tartalom"} src=${this.config?.url||""} style=${`--height:${this.config?.height||350}px`} loading="lazy"></iframe></ha-card>`}}
