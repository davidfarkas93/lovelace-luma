import{LitElement,css,html,nothing}from"lit";import{customElement,property,state}from"lit/decorators.js";import{lumaTokens}from"../styles";import type{HomeAssistant,LovelaceCard}from"../types";
interface Config{type:string;name?:string;controller_entity:string;rain_entity:string;suspended_entity:string;program_entity:string;message_entity?:string;zones:string[]}
@customElement("luma-irrigation-hero-card")export class LumaIrrigationHeroCard extends LitElement implements LovelaceCard{@property({attribute:false})hass?:HomeAssistant;@state()private config?:Config;static styles=[lumaTokens,css`ha-card{position:relative;padding:24px;border:1px solid color-mix(in srgb,var(--tone) 24%,transparent);border-radius:var(--luma-radius-hero);background:linear-gradient(135deg,color-mix(in srgb,var(--tone) 13%,var(--luma-surface)),var(--luma-surface) 68%);box-shadow:var(--luma-shadow)}.top{display:flex;align-items:center;gap:15px}.icon{display:grid;place-items:center;width:58px;height:58px;border-radius:19px;color:var(--tone);background:color-mix(in srgb,var(--tone) 15%,transparent)}.icon ha-icon{--mdc-icon-size:30px}.name{font-size:26px;font-weight:760;letter-spacing:-.035em}.sub{margin-top:4px;color:var(--luma-muted);font-size:13px}.chips{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}.chip{padding:7px 11px;border-radius:999px;color:var(--tone);background:color-mix(in srgb,var(--tone) 11%,transparent);font-size:11px;font-weight:700}@media(max-width:599px){ha-card{padding:18px}.name{font-size:22px}.icon{width:50px;height:50px;border-radius:16px}}`];setConfig(c:Config){if(!c?.controller_entity||!c.zones)throw Error("controller_entity and zones required");this.config={name:"Öntözési központ",...c}}getCardSize(){return 2}render(){if(!this.hass||!this.config)return nothing;const s=this.hass.states,c=this.config,online=s[c.controller_entity]?.state==="on",rain=s[c.rain_entity]?.state==="on",paused=s[c.suspended_entity]?.state==="on",message=c.message_entity&&s[c.message_entity]?.state==="on",active=c.zones.filter(x=>s[x]?.state==="on"),program=s[c.program_entity]?.state||"Nincs adat";const bad=!online||message,tone=bad?"var(--error-color)":paused||rain?"var(--warning-color)":active.length?"var(--success-color)":"var(--primary-color)",icon=!online?"mdi:lan-disconnect":active.length?"mdi:sprinkler":paused?"mdi:pause-circle-outline":rain?"mdi:weather-rainy":"mdi:sprinkler-variant",subtitle=!online?"A vezérlő nem elérhető":active.length?`${active.length} aktív zóna · ${program}`:paused?"Az automatikus indítás felfüggesztve":rain?"Eső miatti tiltás":program;return html`<ha-card style=${`--tone:${tone}`}><div class="top"><span class="icon"><ha-icon icon=${icon}></ha-icon></span><span><div class="name">${c.name}</div><div class="sub">${subtitle}</div></span></div><div class="chips"><span class="chip">${online?"Vezérlő online":"Offline"}</span><span class="chip">${rain?"Eső érzékelve":"Száraz"}</span>${active.length?html`<span class="chip">${active.length} zóna fut</span>`:nothing}${paused?html`<span class="chip">Felfüggesztve</span>`:nothing}</div></ha-card>`}}
